# An example of web page crawling

This example uses a headless Chromium browser. Supports crawling static and dynamic content and scrolling and button pagination.

Usage Example

```
CRAWL_URL=https://web-scraping.dev/products cargo run
```
