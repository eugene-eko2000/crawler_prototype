# An example of web page crawling

This example uses a headless Chromium browser.

It crawls the whole page with static data and scripts loading and waiting for hydration. Supports pagination content crawl with scrolling simulation or "next" button detection and clicking simulation.

Usage Example

```
CRAWL_URL=https://web-scraping.dev/products cargo run
```
